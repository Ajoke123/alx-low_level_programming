#include "main.h"
#include <stdio.h>
/**
 * _abs - function that computes the absolute value of an integer
 * @c: the int that will be used for the argument of the funtion
 *
 * Return: 0
 */
int _abs(int)
{
	if (c >0 || c == 0)
	{
		return (c);
	}
	else
		returm (c * -1);
}
