#include <stdio.h>
/**
 *  *  main - Entry poin
 *  Return: Always 0 (Success)
 */
int main(void)
{
		char str[100] = "\"Programming is like building a multilingual puzzle";

			puts(str);
			return (0);
}
